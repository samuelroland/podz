<div class="flex items-end">
    <div class="text-green text-4xl">
        Podz
    </div>
    <span class="text-sm italic ml-1" title="La version {{ config('app.version') }} a été publiée le {{ config('app.version_date')->format('d.m.Y') }}.">{{ config('app.version') }}</span>
</div>
